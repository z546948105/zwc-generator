version: '3.8'

services:
  zwc-generator:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8899:8899"
    environment:
      - TZ=Asia/Shanghai
      - SPRING_PROFILES_ACTIVE=h2
    volumes:
      - ./data:/app/data
    restart: unless-stopped
